import React from 'react';
import ReactDOM from 'react-dom/client';
function Display()
{
  const dis =()=>{
    alert("GT-650");
  }
  return(<button onClick={dis}></button>);
}
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Display/>);